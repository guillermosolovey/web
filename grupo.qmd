---
title: ""
toc: false
editor: source
freeze: false
---

```{r load_packages, message=FALSE, warning=FALSE, include=FALSE}
library(gsheet)
library(tidyverse)

SHEET_URL <- "https://docs.google.com/spreadsheets/d/1id1P5ke9ZJckr_6ePP84FOLeQPoHb57eitX7yaZ_l8A/edit#gid=1834640090"
```

::: {.content-visible when-profile="en"}
### Current members
:::
::: {.content-visible when-profile="es"}
### Actuales
:::

```{r current-students, results='asis'}
#| echo: false

members <- gsheet2tbl(SHEET_URL)

current <- members %>% filter(fecha == "actual")

for (i in seq_len(nrow(current))) {
  m <- current[i, ]
  proyecto_line <- if (!is.na(m$proyecto) && m$proyecto != "")
    paste0(m$proyecto, "\\\n") else ""
  cat(paste0(
    '::: {.pub-entry .current-member}\n',
    '**', m$nombre, '**\\\n',
    proyecto_line,
    '*', m$carrera, '*\n',
    ':::\n\n'
  ))
}
```

::: {.content-visible when-profile="en"}
### Past members
:::
::: {.content-visible when-profile="es"}
### Anteriores
:::

```{r past-students, results='asis'}
#| echo: false

past <- members %>% filter(fecha == "pasado")

for (i in seq_len(nrow(past))) {
  m <- past[i, ]
  proyecto_line <- if (!is.na(m$proyecto) && m$proyecto != "")
    paste0(m$proyecto, "\\\n") else ""
  cat(paste0(
    '::: {.pub-entry .past-member}\n',
    '**', m$nombre, '**\\\n',
    proyecto_line,
    '*', m$carrera, '*\n',
    ':::\n\n'
  ))
}
```
